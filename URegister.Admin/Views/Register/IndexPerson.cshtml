@using URegister.Core.Models.Register
@model PersonFilterVM
@{
    ViewData["Title"] = "Оторозирани лица";
}
@Html.HiddenFor(x => x.RegisterAdministrationId)
@Html.HiddenFor(x => x.RegisterId)
<div class="ui grid">
    <div class="column">
         <div class="column">
            <table 
                id="persons" 
                class="ui celled striped very compact table" style="width:100%; padding:0px;"
                data-url="@Url.Action("GetPersonList", "Register")">
             </table>
        </div>
    </div>
</div>
@await Html.PartialAsync("_BackButton", @Url.Action("IndexAdministration", "Register", new {registerId = Model.RegisterId}))
@await Html.PartialAsync("_ConfirmDelete")
@section Scripts
{
    <script src="~/js/register/personList.js" asp-append-version="true"></script>
}
